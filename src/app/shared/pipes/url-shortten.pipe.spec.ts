import { UrlShorttenPipe } from './url-shortten.pipe';

describe('UrlShorttenPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlShorttenPipe();
    expect(pipe).toBeTruthy();
  });
});
